## Auth Demo

### Set Up:
- Run:
    - `npm run db:create`
    - `npm run db:reset`
    - `npm install`
    - in `/client`, run `npm install`
- start server with `npm run dev`
- serve react: in `/client` run `npm start`